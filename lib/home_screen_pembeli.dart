import 'package:flutter/material.dart';

class HomeScreenPembeli extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Container(
            
          ),
        ],
      ),
    );
  }
  
}